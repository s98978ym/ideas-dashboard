# Agent Teams アーキテクチャ設計書

> Blog CMS / Financial Model の繰り返しミスを根絶し、堅牢なプロダクト開発を実現するための
> マルチエージェントチーム構成と品質ゲート設計

---

## 1. 背景：なぜ Agent Teams を再設計するのか

### 1.1 Blog CMS で繰り返されたミス

| # | 繰り返しミス | 根本原因 | 影響度 |
|---|-------------|---------|--------|
| **M1** | **メディアアップロード失敗** | ファイルサイズ検証・MIME判定・チャンク処理・リトライの欠如 | **致命的** |
| M2 | XSS/インジェクション脆弱性 | コンテンツサニタイズがフロントのみ | 高 |
| M3 | コンテンツのバージョン管理不在 | 上書き保存のみで差分・ロールバック未実装 | 高 |
| M4 | 画像最適化の欠如 | リサイズ・WebP変換・CDN配信パイプラインなし | 中 |
| M5 | API設計の一貫性崩壊 | REST/GraphQL混在、命名規約なし | 中 |
| M6 | 認証・認可の穴 | ロールベースアクセス制御の不完全実装 | 高 |
| M7 | SEO対応の後回し | メタ情報・OGP・構造化データが散発的 | 中 |
| M8 | キャッシュ戦略の不在 | 全リクエストがDB直撃 | 中 |

### 1.2 Financial Model で繰り返されたミス

| # | 繰り返しミス | 根本原因 | 影響度 |
|---|-------------|---------|--------|
| F1 | 浮動小数点精度エラー | IEEE 754の丸め処理未考慮 | **致命的** |
| F2 | 循環参照による無限ループ | 数式依存グラフの検証なし | 高 |
| F3 | 入力バリデーション不足 | ゼロ除算・負値・境界値の未処理 | 高 |
| F4 | 監査証跡の欠如 | 計算過程のログ未記録 | 高 |
| F5 | シナリオテスト不足 | ベストケースのみ検証、ストレス未実施 | 高 |
| F6 | 規制コンプライアンス違反 | 法規制要件の見落とし | 致命的 |
| F7 | モデル変更のバージョン管理不在 | スキーマ変更の追跡不能 | 中 |
| F8 | 計算チェーンのエラー伝播 | 上流エラーが下流で増幅 | 高 |

### 1.3 共通の構造的欠陥

両プロジェクトに共通する **3つの構造的欠陥**：

1. **検証の後回し** — バリデーションが最終段階にしかなく、問題発見が遅い
2. **責務の曖昧さ** — 誰(どのAgent)が何を保証するのか不明確
3. **回復力の欠如** — 失敗時のリトライ・フォールバック・ロールバック戦略が未設計

---

## 2. Agent Teams 全体アーキテクチャ

```
                    ┌─────────────────────────┐
                    │   Orchestrator Agent     │
                    │   (指揮・判断・ルーティング)    │
                    └────────────┬────────────┘
                                 │
          ┌──────────────────────┼──────────────────────┐
          │                      │                      │
    ┌─────▼─────┐         ┌─────▼─────┐         ┌─────▼─────┐
    │  TEAM α   │         │  TEAM β   │         │  TEAM γ   │
    │ Ingestion │         │ Processing│         │ Delivery  │
    │ & Validate│         │ & Logic   │         │ & Output  │
    └─────┬─────┘         └─────┬─────┘         └─────┬─────┘
          │                      │                      │
    ┌─────▼─────┐         ┌─────▼─────┐         ┌─────▼─────┐
    │  TEAM δ   │         │  TEAM ε   │         │  TEAM ζ   │
    │ Quality   │         │ Security  │         │ Recovery  │
    │ Gate      │         │ & Audit   │         │ & Rollback│
    └───────────┘         └───────────┘         └───────────┘
```

---

## 3. 各チーム詳細設計

### TEAM α — Ingestion & Validation（取り込み・検証チーム）

**ミッション**: すべての入力を受け取り、後続チームが安全に処理できる状態を保証する

#### 所属エージェント

| Agent | 役割 | 対策するミス |
|-------|------|-------------|
| **Media Intake Agent** | ファイル受信・MIME検証・サイズ制限・チャンクアップロード管理 | **M1 (メディアアップロード失敗)** |
| **Schema Validator Agent** | 入力データのスキーマ検証・型チェック・境界値判定 | F3, M5 |
| **Sanitizer Agent** | HTML/SQLインジェクション除去・XSS防止・エスケープ処理 | M2 |

#### Media Intake Agent 詳細設計（M1根絶）

```
メディアアップロード フロー（改善版）:

1. PRE-FLIGHT CHECK
   ├─ Content-Type ヘッダー検証
   ├─ ファイルサイズ上限チェック (設定可能: デフォルト50MB)
   ├─ マジックバイトによる実MIME判定 (拡張子偽装を防止)
   └─ ストレージ残容量チェック

2. CHUNKED UPLOAD
   ├─ 5MB単位のチャンク分割
   ├─ 各チャンクのSHA-256ハッシュ検証
   ├─ アップロード進捗の永続化 (中断再開可能)
   └─ 指数バックオフ付きリトライ (最大3回, 2s→4s→8s)

3. POST-UPLOAD VALIDATION
   ├─ 再組立後のファイル整合性チェック (全体ハッシュ)
   ├─ ウイルススキャン / マルウェア検出
   ├─ メタデータ抽出 (EXIF除去含む)
   └─ サムネイル生成・WebP変換 (非同期)

4. FAILURE RECOVERY
   ├─ 部分アップロードの自動クリーンアップ (TTL: 24h)
   ├─ Dead Letter Queue へのエラー情報保存
   └─ ユーザーへの詳細エラーメッセージ返却
       (「ファイルが大きすぎます」ではなく
        「50MBを超えています(現在: 67.3MB). 圧縮するか分割してください」)
```

**なぜこれで M1 が根絶されるか**:
- Pre-flight で早期に失敗させ、無駄な転送を防ぐ
- チャンク処理で大ファイル・不安定回線に対応
- 中断再開で「途中で切れたら最初から」を排除
- リトライで一時的なネットワーク障害を吸収

---

### TEAM β — Processing & Logic（処理・ロジックチーム）

**ミッション**: ビジネスロジックの正確な実行と、計算の信頼性を保証する

#### 所属エージェント

| Agent | 役割 | 対策するミス |
|-------|------|-------------|
| **Content Transform Agent** | コンテンツの変換・フォーマット統一・リンク検証 | M4, M7 |
| **Calculation Engine Agent** | 金融計算の精度保証・Decimal型強制・丸め規約適用 | **F1** |
| **Dependency Graph Agent** | 数式・参照の依存関係解析・循環検出 | **F2** |
| **Versioning Agent** | コンテンツ/モデルの差分管理・スナップショット | M3, F7 |

#### Calculation Engine Agent 詳細設計（F1根絶）

```
精度保証ポリシー:

1. 型制約
   ├─ すべての金額は Decimal(19,4) で保持
   ├─ float/double の使用を静的解析で禁止
   └─ 通貨ごとの丸めルール定義 (JPY→整数, USD→小数2桁)

2. 計算パイプライン
   ├─ 各ステップで中間結果を監査ログに記録
   ├─ 逆計算による検算 (A×B=C → C÷B≈A の検証)
   └─ 並列計算での結果照合 (2経路計算して一致確認)

3. 境界値防御
   ├─ ゼロ除算 → 明示的 CalculationError(DIVISION_BY_ZERO)
   ├─ オーバーフロー → 閾値超過で自動アラート
   └─ 負値 → コンテキスト別の許可/拒否テーブル参照
```

#### Dependency Graph Agent 詳細設計（F2根絶）

```
循環参照検出フロー:

1. 数式登録/更新時:
   ├─ DAG (有向非巡回グラフ) に変換
   ├─ トポロジカルソートを実行
   ├─ サイクル検出で即座にリジェクト
   └─ 影響範囲の可視化を返却

2. 実行時:
   ├─ 最大再帰深度の強制 (デフォルト: 100)
   ├─ タイムアウト付き計算 (セル単位: 5秒)
   └─ デッドロック検出 → 自動中断 + レポート
```

---

### TEAM γ — Delivery & Output（配信・出力チーム）

**ミッション**: 処理結果を正しい形式・最適な性能でユーザーに届ける

#### 所属エージェント

| Agent | 役割 | 対策するミス |
|-------|------|-------------|
| **API Gateway Agent** | レスポンス整形・ペイロード検証・レート制限 | M5 |
| **Cache Strategy Agent** | マルチレイヤキャッシュ管理・無効化戦略 | M8 |
| **SEO/Meta Agent** | OGP生成・構造化データ・サイトマップ管理 | M7 |
| **Report Renderer Agent** | 帳票・PDF・ダッシュボード出力の生成 | F5 |

#### Cache Strategy Agent 詳細設計（M8根絶）

```
3層キャッシュアーキテクチャ:

L1: Edge Cache (CDN)
    ├─ 静的アセット: TTL 30日 (immutable hash付きファイル名)
    ├─ メディア: TTL 7日 (ETag + If-None-Match)
    └─ HTML: TTL 5分 (stale-while-revalidate: 60s)

L2: Application Cache (Redis)
    ├─ API レスポンス: TTL 動的 (更新頻度ベース)
    ├─ セッション: TTL 24h
    └─ 計算結果: TTL ∞ (入力ハッシュをキーに永続化)

L3: Database Query Cache
    ├─ プリペアドステートメントキャッシュ
    ├─ マテリアライズドビュー (集計系)
    └─ Write-through で自動無効化

無効化戦略:
    ├─ Event-driven: コンテンツ更新時にタグベース一括パージ
    ├─ Cascading: 依存グラフに沿った連鎖無効化
    └─ Grace Period: 古いキャッシュを返しつつバックグラウンド更新
```

---

### TEAM δ — Quality Gate（品質ゲートチーム）

**ミッション**: 各フェーズ間の品質関門として機能し、不良品の通過を阻止する

#### 所属エージェント

| Agent | 役割 | 対策するミス |
|-------|------|-------------|
| **Contract Test Agent** | Agent間のI/F契約テスト・スキーマ互換性検証 | M5, 全体 |
| **Regression Guard Agent** | 変更時の影響分析・回帰テスト自動実行 | 全体 |
| **Scenario Test Agent** | ベスト/ワースト/ストレスシナリオの自動テスト | **F5** |

#### 品質ゲート配置

```
[TEAM α]──── GATE 1 ────[TEAM β]──── GATE 2 ────[TEAM γ]
                │                        │
           入力品質保証              出力品質保証
           ├─ スキーマ適合率 100%     ├─ レスポンス整合性
           ├─ サニタイズ完了確認      ├─ 精度検算パス
           ├─ ファイル整合性確認      ├─ パフォーマンス閾値
           └─ 不正入力リジェクト率    └─ キャッシュヒット率
```

---

### TEAM ε — Security & Audit（セキュリティ・監査チーム）

**ミッション**: 全チームの操作を横断的に監視し、セキュリティと追跡可能性を保証する

#### 所属エージェント

| Agent | 役割 | 対策するミス |
|-------|------|-------------|
| **AuthZ Agent** | ロールベースアクセス制御・権限マトリクス管理 | **M6** |
| **Audit Trail Agent** | 全操作の不変ログ記録・改ざん検知 | **F4** |
| **Compliance Agent** | 規制要件チェック・データ保護ポリシー適合検証 | **F6** |

#### Audit Trail Agent 詳細設計（F4根絶）

```
監査ログスキーマ:

{
  "event_id":    "uuid-v7",           // 時系列ソート可能
  "timestamp":   "2026-02-12T10:30:00.000Z",
  "actor":       "agent:calculation-engine | user:tanaka",
  "action":      "CALCULATE | UPDATE | DELETE | APPROVE",
  "target":      "model:revenue-forecast-v3/cell:B12",
  "input_hash":  "sha256:abc123...",  // 入力の再現性保証
  "output_hash": "sha256:def456...",  // 出力の改ざん検知
  "context": {
    "parent_event": "uuid-of-trigger",
    "calculation_chain": ["B10", "B11", "B12"],
    "precision":   "Decimal(19,4)"
  }
}

保存ポリシー:
  ├─ Append-Only ストレージ (削除不可)
  ├─ 7年間保持 (金融規制準拠)
  └─ 日次で外部バックアップにレプリケーション
```

---

### TEAM ζ — Recovery & Rollback（復旧・ロールバックチーム）

**ミッション**: 障害発生時の自動復旧と、安全なロールバックを保証する

#### 所属エージェント

| Agent | 役割 | 対策するミス |
|-------|------|-------------|
| **Circuit Breaker Agent** | 障害検知・サーキットブレーカー制御・フォールバック | M1, F8 |
| **Rollback Agent** | スナップショートからの状態復元・部分ロールバック | M3, F7 |
| **Dead Letter Agent** | 失敗イベントの収集・分類・再処理 | M1, 全体 |

#### Circuit Breaker Agent 詳細設計（M1+F8根絶）

```
3状態サーキットブレーカー:

[CLOSED] ──失敗率>50%──→ [OPEN] ──クールダウン30s──→ [HALF-OPEN]
    ▲                                                      │
    └──────────────成功──────────────────────────────────────┘
    └──────────────失敗──────→ [OPEN] に戻る

メディアアップロード適用例:
  CLOSED:   通常処理。失敗をカウント
  OPEN:     即座にフォールバック応答を返す
            「現在アップロード処理が混雑しています。
             ファイルはキューに保存されました。
             処理完了時に通知します。」
  HALF-OPEN: 1リクエストだけ試行。成功→CLOSED、失敗→OPEN

エラー伝播防止 (F8対策):
  ├─ 各計算ステップにブレーカーを配置
  ├─ 上流エラー時: 下流計算をスキップ (NaN伝播を防止)
  ├─ 最後の正常値でフォールバック (Stale but safe)
  └─ エラー箇所を赤色ハイライトしてユーザーに提示
```

---

## 4. Agent 間通信プロトコル

### 4.1 メッセージ規約

```
全Agent間通信は以下の統一エンベロープを使用:

{
  "message_id":   "uuid-v7",
  "from_agent":   "media-intake",
  "to_agent":     "content-transform",
  "type":         "REQUEST | RESPONSE | EVENT | ERROR",
  "priority":     "CRITICAL | HIGH | NORMAL | LOW",
  "payload":      { ... },
  "trace_id":     "uuid (リクエスト全体を追跡)",
  "retry_count":  0,
  "deadline":     "2026-02-12T10:31:00Z"
}
```

### 4.2 エスカレーションルール

```
レベル1: Agent内リトライ (3回, 指数バックオフ)
    ↓ 失敗
レベル2: 同チーム内の代替Agentへフォールバック
    ↓ 失敗
レベル3: TEAM ζ (Recovery) へエスカレーション
    ↓ 失敗
レベル4: Orchestrator Agent へ報告 → 人間にアラート
```

---

## 5. メディアアップロード失敗の完全対策マップ

**現状の問題** → **このアーキテクチャでの解決策**

| 失敗パターン | 担当Agent | 対策 |
|-------------|-----------|------|
| ファイルサイズ超過 | Media Intake | Pre-flight で即座にリジェクト + 具体的上限値を提示 |
| MIME偽装 | Media Intake | マジックバイト判定で拡張子に依存しない |
| ネットワーク中断 | Media Intake + Circuit Breaker | チャンク + 中断再開 + 指数バックオフリトライ |
| ストレージ不足 | Media Intake | Pre-flight でディスク残量チェック |
| 同時アップロード過多 | Circuit Breaker | サーキットブレーカー + キュー保存 |
| 部分アップロードの残骸 | Dead Letter | TTL付き自動クリーンアップ |
| 画像処理タイムアウト | Content Transform + Circuit Breaker | 非同期処理 + タイムアウト + フォールバック |
| アップロード後の破損 | Media Intake | SHA-256 ハッシュによる整合性検証 |

---

## 6. ダッシュボードとの連携

本アーキテクチャは、既存のアイデアダッシュボードの知見と直結しています：

| ダッシュボード キーワード | Agent Teams での実現 |
|------------------------|---------------------|
| **自動化 (85%)** | TEAM α の自動バリデーション、TEAM β の自動計算パイプライン |
| **効率化 (78%)** | TEAM γ の3層キャッシュ、TEAM δ の自動回帰テスト |
| **可視化 (65%)** | TEAM ε の監査ダッシュボード、TEAM δ の品質メトリクス |
| **組織変革 (55%)** | Orchestrator による自律的タスク振り分け |
| **顧客価値 (45%)** | TEAM ζ の障害復旧による99.9%可用性 |

---

## 7. 導入ロードマップ

### Phase 1 — 即時対応（1-2週間）
- [ ] Media Intake Agent の実装（M1根絶が最優先）
- [ ] Circuit Breaker Agent の実装
- [ ] 基本的な Audit Trail の導入

### Phase 2 — 基盤構築（3-4週間）
- [ ] TEAM α 全Agent の実装
- [ ] TEAM δ 品質ゲートの配置
- [ ] Calculation Engine の Decimal 型移行

### Phase 3 — 全面展開（5-8週間）
- [ ] 全6チームの完全稼働
- [ ] Orchestrator Agent による自律運用
- [ ] 監査ダッシュボードの構築

---

## 8. 成功指標

| 指標 | 現状（推定） | 目標 |
|------|------------|------|
| メディアアップロード成功率 | ~70% | **99.5%以上** |
| 計算精度エラー | 月数件 | **ゼロ** |
| セキュリティ脆弱性 | 四半期ごとに発見 | **CI/CDで自動ブロック** |
| 障害からの復旧時間 | 手動対応で数時間 | **自動復旧 < 30秒** |
| 監査ログカバレッジ | 部分的 | **全操作100%** |

---

*設計日: 2026-02-12 | 適用対象: Blog CMS / Financial Model / 将来のプロダクト全般*
